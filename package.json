{
  "name": "te-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Top Eleven strategy assistant — static PWA, no dependencies.",
  "type": "module",
  "scripts": {
    "test": "node --test \"test/*.test.js\"",
    "start": "python3 -m http.server 8000"
  }
}
